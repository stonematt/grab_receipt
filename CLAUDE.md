# grab_receipt

## Agent skills

### Issue tracker

Issues and PRDs live in GitHub Issues (`gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

Triage roles map onto the nitimini GitHub Project label scheme (`status: *` + `afk-ready`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
